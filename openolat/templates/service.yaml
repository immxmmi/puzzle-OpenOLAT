apiVersion: v1
kind: Service
metadata:
  name: {{ include "openolat.fullname" . }}
  labels:
    {{- include "openolat.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "openolat.selectorLabels" . | nindent 4 }}
