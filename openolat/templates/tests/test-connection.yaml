apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "openolat.fullname" . }}-test-connection"
  labels:
    {{- include "openolat.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "openolat.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
